#include <iostream>
#include <map>
#include <vector>
#include <map>


class Solution {
public:
    bool isValid(std::string s)
    {
        std::map <char, char> str {
        {
    }
    enum enm
    {
         '(' = ')';
         '{' = '}';
         '[' = ']';
    };
    return 0;
};


int main(int argc, char* argv[])
{
    Solution sln;
    for (std::string str; std::cin >> str; )
    {
        if ( str == "exit" ) break;
        std::cout << sln.isValid(str) << "\n";
    }
}

